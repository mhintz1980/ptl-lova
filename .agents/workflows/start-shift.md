---
description: Run the preflight checks, read current status, and start the development server.
---

1. Install dependencies: `pnpm install`
2. Run preflight checks: `npm run preflight`
3. Read the current project status: Read `docs/status/current-work.md` and display the "Next Actions" to the user.
4. Start the dev server: `pnpm dev`
