---
description: Quick commit and push workflow
---

// turbo-all

1. Stage all changes: `git add -A`
2. Commit with conventional message: `git commit -m "<type>: <message>"`
3. Push to current branch: `git push`
